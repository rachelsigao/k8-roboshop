# Creating a deployment for redis, and it will create a pod with redis container. We use the default configuration of redis, so we don't need to mount any config file or set any environment variable.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: roboshop
  # deployment labels
  labels:
    component: redis
    project: roboshop
    tier: database
spec:
  replicas: 1
  # These are the labels replica set use to create pod replicas, this should match pod labels
  selector:
    matchLabels:
      component: redis
      project: roboshop
      tier: database
  # This is pod definition
  template:
    metadata:
      labels:
        component: redis
        project: roboshop
        tier: database
    spec:
      containers:
      - name: redis
        image: redis:7.0
---

# Create a service to expose redis deployment on port 6379, and it will route traffic to the pods with matching labels.
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: roboshop
  labels:
    component: redis
    project: roboshop
    tier: database
spec:
  selector:
    component: redis
    project: roboshop
    tier: database
  ports:
  - protocol: TCP
    port: 6379 # service port
    targetPort: 6379 # container port